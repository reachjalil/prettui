# @prettui/framework

Typed state, routing, focus, menu, and shell framework for terminal apps.

```bash
npm install @prettui/framework
```

```ts
import { createTuiAppState, reduceTuiAppEvent, renderTuiApp } from "@prettui/framework";
```
