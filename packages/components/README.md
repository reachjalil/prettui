# @prettui/components

Reusable terminal UI panels, cards, tables, status strips, and widgets.

```bash
npm install @prettui/components @prettui/core
```

```ts
import { renderPanel, renderSummaryCards } from "@prettui/components";
```
