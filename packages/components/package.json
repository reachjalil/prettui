{
  "name": "@prettui/components",
  "version": "0.1.1",
  "description": "Reusable terminal UI panels, cards, tables, status strips, and widgets for prettui.",
  "type": "module",
  "license": "Apache-2.0",
  "sideEffects": false,
  "homepage": "https://github.com/reachjalil/prettui",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/reachjalil/prettui.git",
    "directory": "packages/components"
  },
  "bugs": {
    "url": "https://github.com/reachjalil/prettui/issues"
  },
  "keywords": [
    "prettui",
    "terminal",
    "tui",
    "components",
    "dashboard",
    "typescript"
  ],
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "README.md",
    "LICENSE",
    "NOTICE",
    "SECURITY.md",
    "CHANGELOG.md"
  ],
  "scripts": {
    "build": "pnpm run clean && node ../../scripts/build-package.mjs && tsc -p tsconfig.build.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "clean": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\"",
    "pack:dry-run": "pnpm pack --dry-run",
    "prepack": "pnpm run build"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./package.json": {
      "default": "./package.json"
    }
  },
  "dependencies": {
    "@prettui/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "esbuild": "catalog:",
    "typescript": "catalog:"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
